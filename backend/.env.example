DATABASE_URL=postgresql+psycopg2://postgres:admin@localhost:5432/brain_tumor
SECRET_KEY=1234567890abcdef1234567890abcdef
ACCESS_TOKEN_EXPIRE_MINUTES=60
UPLOAD_DIR=./uploads
CORS_ORIGINS=http://localhost:5173
WARMUP_ON_START=false

# Blockchain / IPFS (required for Save to Blockchain feature)
PINATA_API_KEY=your_pinata_api_key
PINATA_SECRET_KEY=your_pinata_secret_key
ETH_PRIVATE_KEY=0xyour_ganache_private_key
FERNET_KEY=your_fernet_key_base64  # generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
