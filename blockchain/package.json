{
  "name": "medical-history-ledger",
  "version": "1.0.0",
  "description": "MedicalHistoryLedger smart contract deployment",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network ganache",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "hh2",
    "hardhat": "^2.22.0"
  },
  "type": "module"
}
